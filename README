
# Ionic Position track app

This is an original project from: https://devdactic.com/ionic-location-tracker-map-track/

Personal example


## Getting Started

* Install the ionic CLI globally: `npm install -g ionic@3.20`
* Install Android studio
* Clone this repository: `git clone https://....
* Run `npm install` from the project root.
* Add platform: ionic cordova platform add ios
* Create the executable: ionic cordova build
* Replace the google API for maps, on "index.html" file search the line: "<script src="http://maps.google.com/maps/api/js?key="></script>" and add your Googke Map Key.
* Run `ionic serve` in a terminal from the project root.



